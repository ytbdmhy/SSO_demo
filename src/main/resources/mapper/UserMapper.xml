<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ytbdmhy.SSO_demo.demo.dao.UserMapper">

    <select id="getUser" parameterType="String" resultType="com.ytbdmhy.SSO_demo.demo.entity.SelfUserDetails">
        SELECT * FROM user
        WHERE username = #{username}
    </select>
</mapper>